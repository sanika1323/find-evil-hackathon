# Find Evil! - UI dependencies
# Install with:  pip3 install --break-system-packages -r requirements.txt
#
# The forensic engine relies on the SANS SIFT Workstation (The Sleuth Kit, etc.)
# and the Claude Code CLI ('claude'), which are provisioned outside pip.

streamlit>=1.32
pandas>=2.0
plotly>=5.20
streamlit-option-menu>=0.3.12